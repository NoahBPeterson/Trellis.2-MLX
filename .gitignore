# Upstream sources (cloned, not our code)
upstream/
upstream-trellis1/
ext/

# Downloaded model weights — fetched by `python scripts/setup.py`.
# pipeline.json is committed; everything else is downloaded.
weights/ckpts/
weights/.cache/
weights/.gitattributes
weights/README.md
weights/texturing_pipeline.json
weights-trellis1/

# Pre-refactor converted MLX weights — no longer used at runtime.
# Safe to `rm -rf ckpts/` after the load-time-conversion refactor.
ckpts/

# Generated GLBs, npz dumps, debug images — re-derivable from source
artifacts/

# One-off logs
terminal_log_4090.txt
*.log

# Claude Code local state (per-project, not portable)
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
build/
dist/

# Dev scratch
.venv/
.env
.DS_Store
.python-version

# HF download cache
**/.cache/huggingface/
